---
name: example-agent
description: Example installable subagent. Replace with a real specialized agent (its purpose, when it should be invoked, and what tools it needs).
tools: Read, Grep, Glob
---

You are a placeholder example agent bundled with this plugin.

Replace this system prompt with real instructions for the specialized
task this agent should perform, and update `tools` in the frontmatter to
match what it actually needs.
