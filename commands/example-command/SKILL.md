---
name: example-command
description: Example of a user-invocable slash command. Replace this with a real task Claude should only run when the user explicitly asks for it (deployments, publishing, anything with side effects or timing the user must control).
disable-model-invocation: true
---

This is a placeholder task. Invoke it directly with `/example-command`
(or `/eakl-agent-skills:example-command` if another installed command
claims the bare name).

Replace this file's body with the actual steps this command should run,
written as explicit numbered instructions.
