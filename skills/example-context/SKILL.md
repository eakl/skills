---
name: example-context
description: Example of a non-user-invocable skill. Replace this with real background knowledge Claude should apply automatically when relevant (conventions, domain facts, style guides). Not exposed as a slash command.
user-invocable: false
---

This is placeholder background knowledge. Claude loads this automatically
when the description above matches the current conversation — there is no
`/example-context` slash command.

Replace this file's content with real reference material: coding
conventions, domain facts, or style guides that should apply without the
user having to ask for them explicitly.
